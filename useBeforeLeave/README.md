# Usage

```javascript
const App = () => {
  const begForLife = () => console.log("Plz don't leave");
  useBeforeLeave(begForLife);
  return (
    <div>
      <h1>usePreventLeave</h1>
    </div>
  );
};
```
